=content_for :nav_links do
  =list_of @services do |service|
    =link_to service, root_path(:service => service)

.page-header
  %h1= @service



#chart

:javascript
  $.getJSON('services/show.json?service=#{@service}', function(data) {
    $.plot($("#chart"), data );
  });
